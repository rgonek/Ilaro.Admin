@model Ilaro.Admin.ViewModels.CellValueViewModel

@if (Model.BoolValue == true)
{
    <i class="glyphicon glyphicon-ok"></i>
}
else if (Model.BoolValue == false)
{ 
    <i class="glyphicon glyphicon-remove"></i>
}
