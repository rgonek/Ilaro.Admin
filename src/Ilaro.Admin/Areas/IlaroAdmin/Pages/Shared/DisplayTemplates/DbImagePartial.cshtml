@model PropertyValue
@{
    if (Model.Raw == null || Model.Raw is byte[] == false)
    {
        return;
    }
}

<img src="data:image/jpg;base64,@Convert.ToBase64String((byte[])Model.Raw)" class="img-thumbnail" />
