@model PropertyValue

<div class="col-md-5">
    @Html.TextArea(Model.Property.Name, Model.AsString, Model.Property, new { @class = "form-control markdown", rows = "10" })
    <span class="help-block" asp-if="@Model.Property.Description.HasValue()">@Model.Property.Description</span>
</div>
@Html.ValidationMessage(Model.Property.Name)
